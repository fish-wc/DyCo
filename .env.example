# ============================================================================ #
# DyCo 环境变量模板
# 复制本文件为 .env 并填入真实密钥。.env 已被 .gitignore 忽略，切勿提交。
#
# 说明：configs/system.yaml 中 model 块的 api_key / model_url 字段填写的
#       就是下面的环境变量【名字】（不是值），运行期由 src/utils/llm_client.py
#       通过 os.getenv() 解析。
# ============================================================================ #

# ---- 主模型（论文主实验使用 GPT-4o-mini，任何 OpenAI 兼容 API 均可）--------- #
OPENAI_API_KEY=your-openai-api-key
# OpenAI 官方: https://api.openai.com/v1 ；兼容 API 请换成对应 base url
OPENAI_BASE_URL=https://api.openai.com/v1

# ---- Embedding 模型（知识库向量化，默认 text-embedding-3-small）------------- #
OPENAI_EMBEDDING_API_KEY=your-embedding-api-key
OPENAI_EMBEDDING_MODEL_URL=https://api.openai.com/v1

# ---- 网络检索（论文使用 JINA）----------------------------------------------- #
# 申请地址: https://jina.ai/
JINA_API_KEY=your-jina-api-key

# ---- 可选：智谱 websearch / 智谱模型（不用可留空）--------------------------- #
# ZHIPU_API_KEY=

# ---- 可选：evaluate/ours/generate_mbti_responses.py 的实验模式开关 ----------- #
# 若需要运行论文中的其他角色组合，请优先通过命令行 --agent_ids 指定；
# 该脚本也保留了若干历史 exp_mode 分支，具体可用值请查看脚本帮助。
# exp_mode=
